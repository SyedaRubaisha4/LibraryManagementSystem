namespace Models.DTOModel
{
    public enum UserStatus
    {
        Active,
        Blocked,
        Pending
    }
    public enum Status
    {
        Active,
        Blocked
    }
    public enum Roll
    {
        Admin,
        User
    }
}
